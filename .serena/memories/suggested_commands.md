# Suggested Commands

## Development
- `yarn dev` - Start in development mode
- `yarn lint` - Run ESLint (required after all changes)
- `yarn build` - Build main and renderer
- `yarn dist` - Full distribution build

## Platform-specific builds
- `yarn dist:win` - Windows build
- `yarn dist:mac` - macOS build
- `yarn dist:linux` - Linux build
